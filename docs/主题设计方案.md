# Ant Design 主题设计方案

## 文档信息

| 项目名称 | 多人屏幕共享系统 |
|---------|-----------------|
| 文档版本 | v1.0 |
| 创建日期 | 2026-01-04 |
| 文档类型 | UI主题设计 |

---

## 1. 主题方案概览

为多人屏幕共享系统设计了两套主题方案，分别采用**天蓝色**和**淡粉色**作为主色调，营造清新、专业的视觉体验。

### 1.1 设计理念

| 设计原则 | 说明 |
|---------|------|
| **清晰易读** | 确保文字和UI元素清晰可见 |
| **视觉舒适** | 长时间使用不疲劳 |
| **专业感** | 符合企业级应用的视觉标准 |
| **品牌识别** | 通过色彩建立产品识别度 |

---

## 2. 主题方案一：天蓝色主题 🌊

### 2.1 设计说明

**设计理念**：天空与海洋的自然色调，传达专业、清新、科技感

**适用场景**：
- 技术团队协作
- 企业级会议
- 开发者工具
- 追求科技感和专业感的场景

**视觉关键词**：专业、清新、科技、信赖

### 2.2 色彩体系

#### 主色调配置

```less
// 主色 - 天蓝色
@primary-color: #40a9ff;           // 主蓝色（亮蓝）
@primary-color-hover: #69c0ff;     // 悬停态
@primary-color-active: #096dd9;    // 激活态
```

**色彩梯度**：
- `#e6f7ff` - 最浅（背景）
- `#bae7ff` - 浅色（辅助背景）
- `#91d5ff` - 淡色（禁用态）
- `#69c0ff` - 亮色（悬停态）
- `#40a9ff` ⭐ 主色
- `#1890ff` - 标准蓝
- `#096dd9` - 深色（激活态）
- `#0050b3` - 较深
- `#003a8c` - 深蓝
- `#002766` - 最深

#### 辅助色配置

```less
// 成功色 - 青绿色
@success-color: #52c41a;

// 警告色 - 琥珀色
@warning-color: #faad14;

// 错误色 - 珊瑚红
@error-color: #ff4d4f;

// 信息色 - 天蓝色
@info-color: #40a9ff;
```

### 2.3 完整Less配置

创建文件：`client/src/renderer/styles/theme-sky-blue.less`

```less
// ============================================
// 天蓝色主题 - 完整配置
// ============================================

// 1. 主色系
@primary-color: #40a9ff;
@primary-1: #e6f7ff;
@primary-2: #bae7ff;
@primary-3: #91d5ff;
@primary-4: #69c0ff;
@primary-5: #40a9ff;
@primary-6: #1890ff;
@primary-7: #096dd9;
@primary-8: #0050b3;
@primary-9: #003a8c;
@primary-10: #002766;

// 2. 功能色
@success-color: #52c41a;
@warning-color: #faad14;
@error-color: #ff4d4f;
@info-color: #40a9ff;
@processing-color: @primary-color;

// 3. 文字颜色
@heading-color: rgba(0, 0, 0, 0.85);
@text-color: rgba(0, 0, 0, 0.65);
@text-color-secondary: rgba(0, 0, 0, 0.45);
@disabled-color: rgba(0, 0, 0, 0.25);

// 4. 背景色
@body-background: #f0f5ff;
@component-background: #ffffff;
@background-color-light: #e6f7ff;

// 5. 边框
@border-radius-base: 6px;
@border-color-base: #d9d9d9;

// 6. 链接
@link-color: #40a9ff;
@link-hover-color: #69c0ff;
@link-active-color: #096dd9;

// 7. 阴影
@shadow-color: rgba(64, 169, 255, 0.15);
@box-shadow-base: 0 2px 8px @shadow-color;

// 8. 布局
@layout-header-background: #001529;
@layout-sider-background: #001529;

// 9. 表格
@table-header-bg: #e6f7ff;
@table-row-hover-bg: #f0f5ff;
```

### 2.4 TypeScript配置对象

创建文件：`client/src/renderer/config/theme-sky-blue.ts`

```typescript
import { ThemeConfig } from 'antd';

export const skyBlueTheme: ThemeConfig = {
  token: {
    // 主色
    colorPrimary: '#40a9ff',
    colorSuccess: '#52c41a',
    colorWarning: '#faad14',
    colorError: '#ff4d4f',
    colorInfo: '#40a9ff',
    
    // 文字
    colorText: 'rgba(0, 0, 0, 0.65)',
    colorTextSecondary: 'rgba(0, 0, 0, 0.45)',
    
    // 背景
    colorBgLayout: '#f0f5ff',
    colorBgContainer: '#ffffff',
    
    // 边框
    borderRadius: 6,
    
    // 字体
    fontSize: 14,
    fontFamily: '-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto',
  },
  components: {
    Button: {
      colorPrimary: '#40a9ff',
      algorithm: true,
    },
    Layout: {
      colorBgHeader: '#001529',
      colorBgBody: '#f0f5ff',
    },
    Table: {
      colorBgContainer: '#ffffff',
      colorFillAlter: '#e6f7ff',
    },
  },
};
```

### 2.5 视觉效果示例

```less
// 应用主容器
.app-container-sky {
  background: linear-gradient(135deg, #f0f5ff 0%, #e6f7ff 100%);
}

// 头部导航
.header-sky {
  background: linear-gradient(to right, #001529, #003a8c);
  box-shadow: 0 2px 8px rgba(64, 169, 255, 0.15);
}

// 卡片样式
.card-sky {
  background: #ffffff;
  border: 1px solid #e6f7ff;
  border-radius: 8px;
  box-shadow: 0 2px 8px rgba(64, 169, 255, 0.12);
  
  &:hover {
    border-color: #91d5ff;
    box-shadow: 0 4px 12px rgba(64, 169, 255, 0.2);
  }
}

// 主按钮
.btn-primary-sky {
  background: linear-gradient(135deg, #40a9ff, #1890ff);
  box-shadow: 0 2px 4px rgba(64, 169, 255, 0.3);
}

// 视频卡片激活状态
.video-card-sky.active {
  border: 2px solid #40a9ff;
  box-shadow: 0 0 0 3px rgba(64, 169, 255, 0.2);
}
```

---

## 3. 主题方案二：淡粉色主题 🌸

### 3.1 设计说明

**设计理念**：温柔优雅的粉色调，传达温馨、友好、亲和力

**适用场景**：
- 创意团队协作
- 设计师会议
- 教育培训
- 追求温馨友好氛围的场景

**视觉关键词**：温馨、友好、优雅、亲和

### 3.2 色彩体系

#### 主色调配置

```less
// 主色 - 淡粉色
@primary-color: #ff85c0;           // 主粉色
@primary-color-hover: #ffadd2;     // 悬停态
@primary-color-active: #f759ab;    // 激活态
```

**色彩梯度**：
- `#fff0f6` - 最浅（背景）
- `#ffd6e7` - 浅色（辅助背景）
- `#ffadd2` - 淡色（悬停态）
- `#ff85c0` ⭐ 主色
- `#f759ab` - 深色（激活态）
- `#eb2f96` - 标准粉
- `#c41d7f` - 较深
- `#9e1068` - 深粉
- `#780650` - 暗粉
- `#520339` - 最深

#### 辅助色配置

```less
// 成功色 - 薄荷绿
@success-color: #52c41a;

// 警告色 - 杏黄色
@warning-color: #faad14;

// 错误色 - 玫瑰红
@error-color: #ff4d4f;

// 信息色 - 淡紫色
@info-color: #d3adf7;
```

### 3.3 完整Less配置

创建文件：`client/src/renderer/styles/theme-pink.less`

```less
// ============================================
// 淡粉色主题 - 完整配置
// ============================================

// 1. 主色系
@primary-color: #ff85c0;
@primary-1: #fff0f6;
@primary-2: #ffd6e7;
@primary-3: #ffadd2;
@primary-4: #ff85c0;
@primary-5: #ff85c0;
@primary-6: #eb2f96;
@primary-7: #c41d7f;
@primary-8: #9e1068;
@primary-9: #780650;
@primary-10: #520339;

// 2. 功能色
@success-color: #52c41a;
@warning-color: #faad14;
@error-color: #ff4d4f;
@info-color: #d3adf7;
@processing-color: @primary-color;

// 3. 文字颜色
@heading-color: rgba(0, 0, 0, 0.85);
@text-color: rgba(0, 0, 0, 0.65);
@text-color-secondary: rgba(0, 0, 0, 0.45);
@disabled-color: rgba(0, 0, 0, 0.25);

// 4. 背景色
@body-background: #fff0f6;
@component-background: #ffffff;
@background-color-light: #ffe6f0;

// 5. 边框
@border-radius-base: 8px;
@border-color-base: #d9d9d9;

// 6. 链接
@link-color: #ff85c0;
@link-hover-color: #ffadd2;
@link-active-color: #f759ab;

// 7. 阴影
@shadow-color: rgba(255, 133, 192, 0.15);
@box-shadow-base: 0 2px 8px @shadow-color;

// 8. 布局
@layout-header-background: #fff0f6;
@layout-sider-background: #fff0f6;

// 9. 表格
@table-header-bg: #fff0f6;
@table-row-hover-bg: #ffe6f0;
```

### 3.4 TypeScript配置对象

创建文件：`client/src/renderer/config/theme-pink.ts`

```typescript
import { ThemeConfig } from 'antd';

export const pinkTheme: ThemeConfig = {
  token: {
    // 主色
    colorPrimary: '#ff85c0',
    colorSuccess: '#52c41a',
    colorWarning: '#faad14',
    colorError: '#ff4d4f',
    colorInfo: '#d3adf7',
    
    // 文字
    colorText: 'rgba(0, 0, 0, 0.65)',
    colorTextSecondary: 'rgba(0, 0, 0, 0.45)',
    
    // 背景
    colorBgLayout: '#fff0f6',
    colorBgContainer: '#ffffff',
    
    // 边框
    borderRadius: 8,
    
    // 字体
    fontSize: 14,
    fontFamily: '-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto',
  },
  components: {
    Button: {
      colorPrimary: '#ff85c0',
      algorithm: true,
    },
    Layout: {
      colorBgHeader: '#fff0f6',
      colorBgBody: '#fff0f6',
    },
    Table: {
      colorBgContainer: '#ffffff',
      colorFillAlter: '#ffe6f0',
    },
  },
};
```

### 3.5 视觉效果示例

```less
// 应用主容器
.app-container-pink {
  background: linear-gradient(135deg, #fff0f6 0%, #ffe6f0 100%);
}

// 头部导航
.header-pink {
  background: linear-gradient(to right, #fff0f6, #ffd6e7);
  box-shadow: 0 2px 8px rgba(255, 133, 192, 0.15);
}

// 卡片样式
.card-pink {
  background: #ffffff;
  border: 1px solid #ffe6f0;
  border-radius: 8px;
  box-shadow: 0 2px 8px rgba(255, 133, 192, 0.12);
  
  &:hover {
    border-color: #ffadd2;
    box-shadow: 0 4px 12px rgba(255, 133, 192, 0.2);
  }
}

// 主按钮
.btn-primary-pink {
  background: linear-gradient(135deg, #ff85c0, #eb2f96);
  box-shadow: 0 2px 4px rgba(255, 133, 192, 0.3);
}

// 视频卡片激活状态
.video-card-pink.active {
  border: 2px solid #ff85c0;
  box-shadow: 0 0 0 3px rgba(255, 133, 192, 0.2);
}
```

---

## 4. 主题切换实现

### 4.1 主题配置管理

创建文件：`client/src/renderer/config/themes.ts`

```typescript
import { ThemeConfig } from 'antd';
import { skyBlueTheme } from './theme-sky-blue';
import { pinkTheme } from './theme-pink';

export type ThemeType = 'sky-blue' | 'pink';

export const themes: Record<ThemeType, ThemeConfig> = {
  'sky-blue': skyBlueTheme,
  'pink': pinkTheme,
};

export const themeNames: Record<ThemeType, string> = {
  'sky-blue': '天蓝色',
  'pink': '淡粉色',
};
```

### 4.2 主题切换组件

```typescript
import { ConfigProvider } from 'antd';
import { useState } from 'react';
import { themes, ThemeType } from './config/themes';

function App() {
  const [currentTheme, setCurrentTheme] = useState<ThemeType>('sky-blue');

  return (
    <ConfigProvider theme={themes[currentTheme]}>
      <YourApp onThemeChange={setCurrentTheme} />
    </ConfigProvider>
  );
}
```

### 4.3 主题选择器

```typescript
import { Radio } from 'antd';
import { ThemeType, themeNames } from '../config/themes';

interface ThemeSelectorProps {
  value: ThemeType;
  onChange: (theme: ThemeType) => void;
}

export function ThemeSelector({ value, onChange }: ThemeSelectorProps) {
  return (
    <Radio.Group value={value} onChange={(e) => onChange(e.target.value)}>
      <Radio.Button value="sky-blue">🌊 {themeNames['sky-blue']}</Radio.Button>
      <Radio.Button value="pink">🌸 {themeNames['pink']}</Radio.Button>
    </Radio.Group>
  );
}
```

---

## 5. 主题对比

### 5.1 视觉效果对比

| 元素 | 天蓝色主题 | 淡粉色主题 |
|------|-----------|-----------|
| **主色** | #40a9ff | #ff85c0 |
| **页面背景** | #f0f5ff（淡蓝） | #fff0f6（淡粉） |
| **头部** | #001529（深蓝） | #fff0f6（淡粉） |
| **按钮** | 蓝色渐变 | 粉色渐变 |
| **卡片边框** | 淡蓝色 | 淡粉色 |
| **阴影** | 蓝色半透明 | 粉色半透明 |
| **视觉感受** | 专业、科技 | 温馨、友好 |

### 5.2 使用场景建议

| 场景类型 | 推荐主题 | 原因 |
|---------|---------|------|
| **技术团队** | 天蓝色 | 专业感强，符合技术氛围 |
| **设计团队** | 淡粉色 | 温馨友好，激发创意 |
| **企业会议** | 天蓝色 | 商务专业，正式感强 |
| **教育培训** | 淡粉色 | 亲和力强，减少压力 |
| **产品演示** | 天蓝色 | 科技感强，突出产品 |
| **团队建设** | 淡粉色 | 轻松氛围，增进感情 |

---

## 6. 实际应用示例

### 6.1 首页配色（天蓝色）

```typescript
import { Card, Tabs, Form, Input, Button } from 'antd';
import styles from './Home.module.less';

export function HomePage() {
  return (
    <div className={styles.containerSky}>
      <Card className={styles.cardSky}>
        <Tabs items={[...]} />
      </Card>
    </div>
  );
}
```

```less
// Home.module.less (天蓝色)
.containerSky {
  background: linear-gradient(135deg, #f0f5ff, #e6f7ff);
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
}

.cardSky {
  background: #ffffff;
  border-radius: 8px;
  box-shadow: 0 2px 8px rgba(64, 169, 255, 0.15);
  
  :global {
    .ant-tabs-tab-active {
      color: #40a9ff;
    }
  }
}
```

### 6.2 房间页面配色（淡粉色）

```less
// Room.module.less (淡粉色)
.containerPink {
  background: linear-gradient(135deg, #fff0f6, #ffe6f0);
}

.headerPink {
  background: linear-gradient(to right, #fff0f6, #ffd6e7);
  border-bottom: 2px solid #ffadd2;
}

.videoCardPink {
  border: 2px solid #ffe6f0;
  
  &.active {
    border-color: #ff85c0;
    box-shadow: 0 0 0 3px rgba(255, 133, 192, 0.2);
  }
}
```

---

## 7. 配色工具和资源

### 7.1 在线调色工具

- **Ant Design Colors**: https://ant.design/docs/spec/colors-cn
- **Coolors**: https://coolors.co/
- **Adobe Color**: https://color.adobe.com/

### 7.2 色彩无障碍检查

确保两套主题都符合WCAG 2.0 AA标准：

```typescript
// 对比度检查（文字与背景）
天蓝色主题：
- 标题黑色 vs 白色背景：21:1 ✅
- 正文灰色 vs 白色背景：7:1 ✅
- 主色按钮白字 vs 蓝色背景：4.5:1 ✅

淡粉色主题：
- 标题黑色 vs 白色背景：21:1 ✅
- 正文灰色 vs 白色背景：7:1 ✅
- 主色按钮白字 vs 粉色背景：4.5:1 ✅
```

---

## 8. 使用建议

### 8.1 默认主题选择

建议将**天蓝色主题**设置为默认主题，原因：
1. 适用场景更广泛
2. 专业感更强
3. 符合大多数企业应用的视觉期望

### 8.2 主题本地存储

```typescript
// 保存用户选择的主题
const saveTheme = (theme: ThemeType) => {
  localStorage.setItem('app-theme', theme);
};

// 读取用户选择的主题
const loadTheme = (): ThemeType => {
  return (localStorage.getItem('app-theme') as ThemeType) || 'sky-blue';
};
```

### 8.3 主题切换动画

```less
// 添加平滑过渡
.theme-transition {
  transition: all 0.3s ease-in-out;
}
```

---

**文档版本**: v1.0  
**最后更新**: 2026-01-04  
**维护者**: 项目团队
